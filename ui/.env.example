# Gốc API (Nest), không nối /api ở đây — client tự nối path /api/...
# Cùng máy dev: http://localhost:3000
# Production: https://api-spa.dosutech.site
#
# Để trống: môi trường dev dùng Vite proxy (server.proxy trong vite.config.ts) → yêu cầu tới cùng origin /api
VITE_API_BASE_URL=http://localhost:3000
